hashset底层原理：
    1、会默认创建一个数组，容量为16，加载因子为0.75，数组名为table
    2、根据元素的哈希值和数组容量计算出元素的存放位置
    3、如果该位置为null，则直接存入
        如果不为null，则直接添加到该位置下，形成链表（第一次添加元素时）
    4、当该数组的存的元素数量达到16 * 0.75 = 12时，则扩充数组
    5、当链表长度大于8及数组长度大于64，则链表自动转化为红黑树
    6、如果集合存的自定义对象，那么必须自己重写hashCode和equals方法，否则只会根据默认的地址值进行计算
