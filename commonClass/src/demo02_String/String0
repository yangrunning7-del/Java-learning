String类型：是不可变的字符序列，且不可以被继承，final class

String name = "miyang";
这句话会在本地方法区的常量区，去搜索有没有内容为miyang的常量区，
如果没有，就创建一个区域用于存放miyang，这个区域的地址被栈内容的name所指，miyang在常量区的内容本身是不可改的

如果后面
name += "1111";
那么，本地方法区的常量池就会区搜索有没有内容为miyang1111的常量
如果没有，就再创建一个区域用于存放miyang1111，栈内存中name所指的的地址更新为存放miyang111的常量池地址
